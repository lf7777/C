1.声明只读数组:
用const声明数组
const int days[months]={1,1,1,1,1,1,1,11};

2.使用数组，最好定义预处理变量#define 30
循环中直接使用

3.c中，指针+1是指增加一个存储单元。
对数组来说，这意味着加1后的地址是下一个元素的地址，而不是下一个字节的地址，这是为什么必须声明指针所指向对象类型的原因之一。只知道地址并不够，因为计算机要知道储存对象需要多少字节

4.dates+2 = &date[2]
*(dates+2) = date[2]

5.*(dates +2) dates第三个元素的值
  *dates +2 dates第一个元素的值+2
